# Streamlit Community Cloud dependencies for cloud/streamlit/streamlit_app.py.
#
# Streamlit Cloud reads the requirements file next to the entry point before
# the one at the repository root (which belongs to the legacy prototype).
# Same set as cloud/api/requirements.txt, with Streamlit in place of the
# FastAPI web layer. Select Python 3.12 when deploying.

streamlit>=1.40,<2

# --- configuration & typed models ---
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
python-dotenv>=1.0
requests>=2.32,<3

# --- database layer (imported by the engine; disabled with DATABASE_BACKEND=none) ---
psycopg[binary]>=3.2,<4
SQLAlchemy>=2.0.36,<3
sqlglot>=25.0,<31

# --- vector store & retrieval ---
qdrant-client>=1.12,<2
rank-bm25>=0.2.2

# --- inference ---
ollama>=0.4
fastembed>=0.5,<1

# --- document ingestion ---
pypdf>=5.0
python-docx>=1.1
markdown-it-py>=3.0
PyYAML>=6.0

# --- data ---
pandas>=2.2
numpy>=1.26

# --- observability ---
structlog>=24.4
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
