{
  "name": "copilot-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker API gateway for the Enterprise Intelligence Copilot",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260922.1",
    "typescript": "^5.7.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.136.2"
  }
}
