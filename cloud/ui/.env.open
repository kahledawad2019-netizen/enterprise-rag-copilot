# Build mode for the local app and the public demo: no sign-in provider.
# `npm run build:open` loads this file. It outranks .env.local, so a Clerk
# key kept there for the staging gateway build does not leak into this one.
# Nothing in this file is secret.
VITE_AUTH_PROVIDER=none
VITE_CLERK_PUBLISHABLE_KEY=
